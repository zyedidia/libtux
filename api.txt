libvisor api:

visor_init(options{
    callbacks: {
    }
})

visor_exec(program)


callbacks:

plat_make_vaspace()
plat_make_context()

context_resume()

va_mmap(vaspace, ...)
va_munmap(vaspace, ...)
va_mprotect(vaspace, ...)

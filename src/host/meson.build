if host == 'linux'
  srcs += files(
    'linux/random.c',
    'linux/dir.c',
    'posix/file.c',
  )
elif host == 'posix'
  srcs += files(
    'posix/random.c',
    'posix/file.c',
    'posix/dir.c',
  )
endif
